{
  // enum_subset_attachment_inline_enum.contract.stvn
  :defs {
    :include [ "error_contract.stvn_inclf" ]
  }

  :type :ErrorContract

  :body (
    #MALFORMED_SCHEMA
    "Constraint violation (:IllegalInline): filter facets cannot be applied to inline enum constructors; filter facets are only allowed on nominal aliases of :Enum or existing enum subsets"
    "org.stvnadore.core.validation.MalformedSchemaException"
  )
}
